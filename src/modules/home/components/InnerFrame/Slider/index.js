import ServerSlider from "./ServerSlider";
import ClientSlider from "./ClientSlider";

const Slider = async () => {
    return (
        <ServerSlider>
            {/* <p>abc</p> */}
            <ClientSlider />
        </ServerSlider>
    );
};

export default Slider;
